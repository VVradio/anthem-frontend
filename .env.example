# Copy to ".env" for local dev, or set this in Vercel/Netlify project settings.
# Leave empty for demo mode (agents call AI directly).
# Set to your deployed backend URL to go live (login + server-side AI key).
VITE_API_BASE=
